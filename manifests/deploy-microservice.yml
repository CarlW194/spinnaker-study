kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: teamone-microservice
        stack: frontend
        source: demo
    name: teamone-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: teamone-microservice
    template:
        metadata:
            labels:
                app: teamone-microservice
                run: teamone-microservice
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/teamone-microservice
                    name: teamone-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: teamone-microservice-service
    name: teamone-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: teamone-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: teamone-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /teamone(/|$)(.*)
                        backend:
                            serviceName: teamone-microservice-service
                            servicePort: 30201


